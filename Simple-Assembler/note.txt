ERRors:
done 1) Typo
if inst[0] not in op_code or inst[1] not in reg:
    print("typo_error in inst")
    break;

done 2)undefined variables
undefined variables means you have not defined or declared the varaible and using it
done 3)undefined lables
undefined variables means you have not defined or declared the varaible and using it

done 4)Illegal use of FLAGS register
For mov and load instruction
if inst[1] == FLAGS:
    print(Illegal use of FLAGS register)

done 5)Illegal Immediate values 
if inst[2][1:]>255 or <0:
    print(Illegal Immediate values )
    break;

done 6)Misuse of labels as variables or vice-versa
when we encounter label 
if label is in variables:
    print(error)

when we encounter variables 
if variables is in labels:
    print


done 7)Missing hlt instruction
if "hlt" not in inst_list:
    print(Missing hlt instruction)
    break;

done 8)hlt not being used as the last instruction

done 9)Wrong syntax used for instructions (For example, add instruction being used as a
type B instruction )

10) same label name used more than one time
eg: loop1: mov R1 R2
    loop1: mov R3 $4

typ A = 4
typ B,C,D = 3
typ E = 2
typ F = 1

